# ROCKETS オフィシャルサイト ── 取扱説明書

---

# ① まず Vercel で公開する（5分・無料・登録以外の準備ゼロ）

## 手順

1. ブラウザで **https://vercel.com/drop** を開く
2. GitHub か Google か メールでログイン（無料アカウント）
3. **この `website` フォルダを、そのページにドラッグ＆ドロップ**
   - Finder で `website` フォルダごと掴んで、ブラウザの画面に落とすだけ
   - ⚠️ フォルダの「中身」ではなく、**`website` フォルダ自体**をドロップ
4. プロジェクト名を決める（例 `rockets`）→ **Deploy**
5. 30秒ほどで `https://rockets-xxxx.vercel.app` のようなURLが出る → **これがサイト**

Git も、ターミナルも、コマンドも一切いりません。

## 出てきたURLは、誰にでも共有できます

Vercel の **無料プラン（Hobby）では、本番URL（`xxx.vercel.app`）は最初から誰でも見られる公開状態**です。
ログイン不要。URLをLINEやX、DMに貼ればそのまま開けます。

- **スマホでもそのまま見られます。** レイアウトはスマホ用に自動で切り替わります
  （メニューは右上の三本線、写真は全画面でスワイプ送り、下スワイプで閉じる）
- **もし「ログインしてください」と出たら**、Vercel のプロジェクト画面 →
  **Settings → Deployment Protection → Vercel Authentication を Disabled** にする

### SNSに貼ったときのサムネイル

URLが確定したら、`index.html` の上のほうにある2箇所を、フルURLに書き換えると確実にサムネイルが出ます。

```html
<meta property="og:image" content="https://自分のURL/assets/photos/p084.jpg">
```

（面倒ならやらなくても大丈夫。サイト自体は問題なく見られます）

## 更新するとき

`content.js` を書き換えたら、**もう一度 vercel.com/drop にドロップし直す**。
（同じプロジェクト名を選べば上書きされます）

## 独自ドメインを付けたい場合

`rockets-band.com` のようなドメインを取得（お名前.com / Cloudflare など、年1,000〜2,000円）
→ Vercel のプロジェクト画面 → **Settings → Domains** → ドメインを入力して、指示通りDNSを設定。

---

# ② 中身を書き換える

**編集するのは `content.js` の1ファイルだけです。** それ以外は触らなくて大丈夫。

`content.js` を右クリック →「このアプリケーションで開く」→ **テキストエディット**（またはVS Code）
書き換えて保存 → `index.html` をブラウザで開いて **⌘R** で確認 → よければ Vercel に再ドロップ。

> ⚠️ `"` （ダブルクォート）と `,` （カンマ）は消さないこと。
> 消すとページが真っ暗になります。戻して保存し直せば直ります。

---

## いま「仮」で入っているもの（要差し替え）

| 場所 | 状態 |
|---|---|
| ライブ情報（3件） | ダミー。実際の日程に書き換える |
| SNSのURL | トップページに繋がってるだけ。自分のアカウントURLに |
| メンバー名・パート | 「メンバー名」のまま。写真は仮で4枚割り当て済み |
| バンド紹介文 | 仮の文章 |
| 映像 | 未設定 → セクションごと非表示中 |
| 音源 | 未設定 → セクションごと非表示中 |

---

## よくある編集

### ライブを追加する

`content.js` の `lives:` の中に、`{ ... },` のかたまりをコピペして増やす。

```js
{
  date:   "2026.10.03",
  day:    "FRI",
  title:  "イベント名",
  venue:  "下北沢SHELTER",
  open:   "18:00",
  start:  "18:30",
  adv:    "¥2,500",
  door:   "¥3,000",
  note:   "＋1D",
  ticket: "https://ここに予約フォームのURL"
},
```

- **一番上に書いたライブが、トップの大きい「NEXT LIVE」に出ます**
- 終わったライブは `past: true,` を1行足せば「過去の公演」に移動
- `ticket: ""` にすればチケットボタンが消える

### メンバーの写真を変える

`_管理用/写真一覧.jpg` を開くと、採用した31枚と番号が一覧で見られます。
使いたい写真の番号（例 `p203`）を `photo: "p203.jpg"` のように書く。

### ライブ映像を載せる

YouTube にアップして、URLの `v=` の後ろの文字列をコピー。

```
https://www.youtube.com/watch?v=AbCdEfGhIjK
                                 ↑ここ
```

```js
videos: [
  { id: "AbCdEfGhIjK", title: "2026.06.21 渋谷" }
],
```

書いた瞬間、非表示だった「映像」セクションが出てきます。

### スタジオ音源を載せる

1. mp3 を `website/assets/audio/` フォルダに入れる
2. `content.js` に書く

```js
tracks: [
  { title: "曲名", file: "take1.mp3", note: "スタジオ録音 2026.05" }
],
```

### 演出の速さを変える

`content.js` の一番下。

```js
settings: {
  fadeSeconds: 6,      // 写真が切り替わる間隔（秒）
  crossfade: 2.4,      // フェードの長さ（秒）長いほどゆっくり
  kenBurns: true,      // ゆっくり寄る動き。false で完全静止
  tintStrength: 0.55   // 写真の色がページに乗る強さ（0〜1）
}
```

---

## 採用した写真について（50枚）

`_管理用/写真一覧.jpg` に全50枚と番号が載っています。
`*` = あなたが指定した13枚 ／ `H` = トップ背景に流れるもの

- **指定された13枚（必ず収録）**
  `p002 p017 p026 p044 p074 p093 p094 p097 p143 p167 p175 p178 p179`
- **こちらで選んだ37枚** … アー写・ライブ（カラー）・ライブ（モノクロ）をバランスよく
- **ロゴだけ／ロゴが主役のポスター画像は全部除外しました**
  （`p233` `p055` `p078` `p146` `p163` `p174` `p031` `p066` `p091` `p201` `p224` `p000` など）
  ロゴは切り抜いてサイトのロゴとして使っているので、写真ページには出しません
- **トップ背景に流れるのは横位置の31枚。** 6秒ごとにランダムでフェード

写真を差し替えたい／追加したいときは、元の写真を `ROCKETs` フォルダに入れて「差し替えたい」と言ってください。

---

## 中身の構成

```
website/                ← ★これをVercelにドロップ
├── index.html          ← サイト本体（さわらない）
├── content.js          ← ★ここだけ編集する
├── vercel.json         ← 公開設定（さわらない）
├── はじめに読む.md      ← このファイル
├── _管理用/
│   └── 写真一覧.jpg     ← 採用50枚の番号一覧
└── assets/
    ├── brand/          ← ロゴ（p233から切り出した透過PNG）
    ├── photos/         ← 表示用 50枚
    ├── thumbs/         ← 一覧用 50枚
    ├── photos.js       ← 写真の情報と色データ
    └── audio/          ← ここに mp3 を入れる
```
